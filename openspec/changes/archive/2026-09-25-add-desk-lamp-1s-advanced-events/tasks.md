# Tasks

## 1. 参数化事件配置与兼容性

- [x] 1.1 在 `automation/src/iot_exp/models.py` 增加亮度、色温、情景和专注目标事件及页面观察数据结构；用模型测试验证四种目标的类型、范围、单位、误差和非法目标拒绝。
- [x] 1.2 将实验事件唯一性改为按 `(event_type, target)` 判定，并规定新旧事件的前置条件；用配置测试验证六个不同情景目标可共存、重复同一目标被拒绝，以及旧台灯和音箱 YAML 原样加载。
- [x] 1.3 在 `automation/README.md` 记录新事件 YAML 字段和“目标已达到不算状态变化”的语义；用一份有效和一份越界的配置示例核对文档与解析结果一致。

## 2. 编排、证据与会话校验

- [x] 2.1 扩展运行器的候选选择和计划计数，使不同目标分别调度，动作前再次读取相关维度，且目标已达到时改选合法事件；用模拟测试覆盖两档亮度往返、六个情景目标和等待期间状态变化。
- [x] 2.2 扩展运行器的参数化动作与回执判定：仅在后读值命中目标且与前读值不同的情况下记录 `app_ack_only`；用模拟测试覆盖成功、同值、未知、超时和无合法后继事件，并确认不会无限循环。
- [x] 2.3 扩展动作记录和页面证据引用，保存每次尝试的目标、前后观察、时间、来源、命令范围及前后 XML/截图；用会话测试核对文件与 `event_id` 对应、重试 ID 唯一、失败证据不被覆盖，旧记录仍可读取。
- [x] 2.4 扩展 `validate-session` 和质量报告，对新事件检查目标与前后值、误差、时间顺序、证据文件及计划身份计数；用篡改后的测试记录证明缺失后读值或错误成功分类会被拒绝，同时旧会话校验保持通过。
- [x] 2.5 在 `automation/README.md` 写明新增日志字段、证据级别及校验方法；用一份新事件模拟会话核对文档描述与 `actions.jsonl`、质量报告和校验输出一致。

## 3. 台灯页面发现与四类控制

- [x] 3.1 扩展台灯的只读页面检查，使设备页、六个情景状态和专注二级页可导出 XML/截图及可读状态；在真机可用时据此记录 App 版本、亮度/色温实际范围与单位、六个按钮标识、专注开关属性，无法核实的字段保持未配置并禁止真实动作。
- [x] 3.2 在台灯适配器实现亮度滑动与回读，按实测范围和步进定位目标；用假驱动测试验证边界、反向调节、误差与不可读状态，真机可用时用一次前后页面证据核对结果。
- [x] 3.3 在台灯适配器实现色温滑动与回读，并把实际单位写入观察；用假驱动测试验证上下界、单位、误差和超时，真机可用时用一次前后页面证据核对结果。
- [x] 3.4 在台灯适配器实现六个情景按钮与唯一亮度/色温预设的一一映射；用假驱动逐一验证六个目标的数值回读，真机保存每种模式的页面证据，无法辨认时不得报成功。
- [x] 3.5 在台灯适配器实现专注模式二级页导航、开关前后读取和返回设备页；用假驱动测试开/关、页面缺失、导航恢复，真机可用时保存两种目标的前后证据。
- [x] 3.6 新建台灯高级事件实验 YAML，配置经页面证据核实的范围、误差、六个情景 ID 和选择器；验证配置加载及新旧台灯模板并存，并在 `automation/README.md` 标明已核实的 App 版本、选择器来源和未验证能力。

## 4. 开发入口与呈现

- [x] 4.1 扩展模拟适配器和 CLI 干运行入口，使高级事件在不连接真机时产生完整目标、前后状态和证据；运行高级模板的 `--dry-run` 与 `validate-session`，确认全部计划事件完成且无 HA 日志要求。
- [x] 4.2 更新控制台模板与历史展示，使同类事件的不同目标显示为亮度/色温数值、情景名称和专注开关目标，并明确 `app_ack_only`；运行对应前端测试或页面检查以及 `npm run web:build`，确认构建后的页面不把六个情景显示为同名事件。

## 5. 跨模块验收

- [x] 5.1 在 `automation/` 运行 `ruff` 和必要的 `pytest`，并执行旧台灯、音箱及新台灯模板的干运行与会话校验；记录各自会话 ID、结果和任何仍未核实的真实页面字段。
- [x] 5.2 若目标手机和台灯可用，使用无抓包开发配置逐类执行最小真机闭环，逐条核对操作日志、前后页面状态与证据文件；保留失败会话，新建会话重试，并明确报告 `app_ack_only` 与未完成项。

## 6. 无截图情景回执与审计修复

- [x] 6.1 逐一触发六种情景并记录对应亮度/色温回读，证实按钮 XML 不暴露选中属性；配置唯一预设组合，运行时仅读取数值进行判定。
- [x] 6.2 动作前回读未知时禁止发送命令，成功记录要求完整时间戳及证据引用，会话计划未做满时标为 `incomplete`。
- [x] 6.3 混合事件先完成可执行滑块目标再选择情景，修复 Windows 网页测试入口，运行回归测试与干运行校验。
