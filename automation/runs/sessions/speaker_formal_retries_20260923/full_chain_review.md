# 小爱触屏音箱播放/暂停全链路核对

会话：`speaker_formal_retries_20260923`
HA 实体：`media_player.xiaomi_cn_636575596_lx04`
音箱热点地址：`10.42.0.196`
时间：2026-09-23，以下均为北京时间（UTC+8）。

## 第一次播放

- 米家 App 点击开始：17:35:24.218；点击结束：17:35:25.938。
- HA 历史记录：17:35:26.705，实体变为 `playing`；校正实测时钟差后，约为点击开始后 2.46 秒。
- 米家操作后截图 `screenshots/speaker_formal_retries_20260923_event_000001_attempt_01.png`：按钮显示暂停图标，页面处于播放状态。截图保留在采集机本地。
- `traffic.pcapng` 中该操作窗口内，音箱发出 61 个包、收到 53 个包；例如 17:35:25.575 有音箱发出的 TCP 数据包，17:35:26.566 有发往音箱的 TCP 数据包。

## 第一次暂停

- 米家 App 点击开始：17:35:35.653；点击结束：17:35:36.800。
- HA 历史记录：17:35:38.003，实体变为 `paused`；校正实测时钟差后，约为点击开始后 2.32 秒。
- 米家操作后截图 `screenshots/speaker_formal_retries_20260923_event_000002_attempt_01.png`：按钮显示播放图标，页面处于暂停状态。截图保留在采集机本地。
- `traffic.pcapng` 中该操作窗口内，音箱发出 69 个包、收到 60 个包。

## 数据与边界

- 本次会话原始动作记录：`actions.jsonl`；原始抓包：`traffic.pcapng`；HA 原始导出：`ha_history.json`；时钟实测：`ha_clock_probe.json`；自动关联：`ha_reconciliation.json`；包计数：`pcap_review.json`。
- HA 电脑在导出时测得采集机时间减 HA 时间为 −27.718 毫秒，测量不确定度不超过 1.228 毫秒。
- 整段会话有 40 个播放/暂停状态变化与动作记录一一对应，HA 历史文件另含查询起点的初始 `paused` 状态。5 次点击未立即得到 App 回执，随后重试；原始记录均保留。
- 抓包证明操作时段内有音箱双向流量。流量承载的具体控制指令若经过加密，仅凭这些数据包不能直接读取；上述闭环判断结合了 App 页面状态、HA 实体状态及抓包时间。
